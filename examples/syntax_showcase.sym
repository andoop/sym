// 语法糖示例（默认与 prelude 一起加载时可 `sym run`）

fn demo(n: Int,) -> Int =
  if n < 0 then
    +n
  else if n < 1_0 then
    n * 2
  else
    n
end

fn main() -> Unit =
  do
    println(demo(-3,));
    println(demo(4,));
    ()
  end
end
